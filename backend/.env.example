# Copy to .env in this same folder (backend/.env). The app does NOT read .env.example.
# Local MySQL (Workbench): use your real user/password. Docker: match repo root .env.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=eqwatch
